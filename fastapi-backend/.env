# # Generic OpenID Provider (any OIDC-compliant server: Keycloak/Okta/Dex/Cognito)
# OIDC_ISSUER=https://<issuer-domain>/.well-known/openid-configuration
# # Accept multiple audiences if needed, comma-separated
# OIDC_AUDIENCES=api://default,vue-client
# # Optional: allow multiple trusted issuers (multi-tenant or migration)
# OIDC_EXTRA_ISSUERS=https://issuer-2/.well-known/openid-configuration

# # Role mapping preferences (provider differences)
# OIDC_ROLE_PATHS=realm_access.roles,claims.roles,groups
# # Cache TTLs
# OIDC_DISCOVERY_CACHE_SEC=3600
# OIDC_JWKS_CACHE_SEC=3600

OIDC_ISSUER_URL=http://localhost:8080/realms/demo-realm
OIDC_CLIENT_ID=vue-client
OIDC_AUDIENCE=
OIDC_REQUIRED_ROLES=dashboard
OIDC_ROLE_SOURCES=realm,resource,groups,roles
OIDC_RESOURCE_CLIENT_ID=vue-client
OIDC_VERIFY_AUD=false
OIDC_VERIFY_ISSUER=true
OIDC_ACCEPTED_ISSUERS=http://keycloak:8080/realms/demo-realm
